export interface Position {
  x: number
  y: number
}

export enum Location {
  Top = 'top',
  Right = 'right',
  Left = 'left',
  Bottom = 'bottom',
}

export enum Direction {
  Up = 'up',
  Down = 'down',
  Left = 'left',
  Right = 'right',
}
